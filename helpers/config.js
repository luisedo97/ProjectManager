const config = {
    dbUrl: 'postgres://postgres:masterkey@localhost:5432/Projectmanager',
    port: 3001,
    secret: 'Vu1Kn0',
    uploads: `${__dirname}/../public/uploads`
}

module.exports = config;